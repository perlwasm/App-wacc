#!/usr/bin/env perl

use strict;
use warnings;
use lib::findbin '../lib'; # dev-only
use App::wacc;

# PODNAME: wacc
# ABSTRACT: WebAssembly C compiler wrapper for clang
# VERSION

exit App::wacc->main(@ARGV);
