#!/usr/bin/env perl

use strict;
use warnings;
use lib::findbin '../lib'; # dev-only
use App::wa2json;

# PODNAME: wacc
# ABSTRACT: WebAssembly C compiler wrapper for clang
# VERSION

exit App::wa2json->main(@ARGV);
